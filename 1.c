#include <stdio.h>

int main()
{
    int x;
    int sum;
    
    while(1)
    {
        if(x % 2 == 0)
        {
            sum += x;
        }
        x++;
        if(x=1, x<=10, x++)
        {
            sum += x;
            break;
        }
    }
}